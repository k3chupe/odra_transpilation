[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "odra-router"
version = "0.1.0"
description = "Custom routing and optimization stages for the ODRA5 (IQM Adonis) topology, benchmarked against Qiskit"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "qiskit==1.2.4",
    "qiskit-aer==0.15.1",
    "numpy>=1.26",
]

[project.optional-dependencies]
hardware = [
    "iqm-client[qiskit]==29.14.0",
    "iqm-data-definitions==2.15",
    "iqm-exa-common==26.33.0",
    "iqm-station-control-client==9.8.0",
]
ilp = ["gurobipy"]
analysis = ["pandas", "plotly", "tqdm", "matplotlib"]
dev = ["pytest>=8", "nbstripout>=0.7", "ipykernel", "ipywidgets", "jupyter"]

[project.scripts]
odra-router-bench = "odra_router.bench:main"
odra-router-bench-queko = "odra_router.bench:main_queko"
odra-router-bench-sweat = "odra_router.bench:main_sweat"
odra-router-bench-fidelity = "odra_router.bench:main_fidelity"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
